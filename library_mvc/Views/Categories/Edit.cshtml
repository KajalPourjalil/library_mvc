@model Category

@section title {
    @if (Model != null)
    {
        <h3>Title: @Model.Name</h3>
    }else{
        <h3>Not found</h3>
    }
}

@if (Model != null)
{
    <partial name="_Category" model="@Model"/>
}